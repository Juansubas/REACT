import React, { ButtonHTMLAttributes } from 'react'

type Props = {
    
}

function Button({}: ButtonHTMLAttributes<HTMLButtonElement>) {
    return(
        <button>Button</button>
    )
}

export default Button
